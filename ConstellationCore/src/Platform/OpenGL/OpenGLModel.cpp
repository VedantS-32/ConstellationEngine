#include "CStellpch.h"
#include "OpenGLModel.h"

namespace CStell
{
  //  std::vector<OpenGLTexture2D> OpenGlModel::loadMaterialTextures(aiMaterial* mat, aiTextureType type, std::string typeName)
  //  {
		//std::vector<OpenGLTexture2D> textures;
		//for (unsigned int i = 0; i < mat->GetTextureCount(type); i++)
		//{
		//	aiString str;
		//	mat->GetTexture(type, i, &str);
		//	auto& texture = Texture2D::Create(str.C_Str());
		//	textures.push_back(*texture);
		//}
		//return textures;
  //  }
}
